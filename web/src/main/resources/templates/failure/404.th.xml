<?xml version="1.0"?>
<html lang="en" xmlns:th="http://www.thymeleaf.org/">
    <thlogic>
        <attr sel="footer"
              th:replace="~{_snippets/footer :: footer}" />
        <attr sel="#title"
              th:text="#{web.resources.templates.failure.404.title}"/>
        <attr sel="failure_code"
              th:text="#{web.resources.templates.failure.404.code}"/>
        <attr sel="#failure_name"
              th:text="#{web.resources.templates.failure.404.name}"/>
        <attr sel="#failure_description"
              th:text="#{web.resources.templates.failure.404.description}"/>
        <attr sel="#href_index"
              th:text="#{web.resources.templates.failure.go_home}"/>
    </thlogic>
</html>
