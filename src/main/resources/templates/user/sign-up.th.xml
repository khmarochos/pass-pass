<?xml version="1.0"?>
<html lang="en" xmlns:th="http://www.thymeleaf.org/">
    <thlogic>

        <attr sel="footer"
                th:replace="~{_snippets/footer :: footer}" />

        <attr sel="#title"
              th:text="#{registration.title}"/>

        <attr sel="#please_register_m1"
              th:text="#{registration.please_register_m1}"/>

        <attr sel="#please_register_m2"
              th:text="#{registration.please_register_m2}"/>

        <attr sel="#registration_form"
              th:action="@{${T(ua.tucha.passpass.util.RouteRegistry$UserRouteRegistry).SIGN_UP}}"
              th:object="${userDTO}">

            <attr sel="#name_field"
                  th:field="*{name}"
                  th:placeholder="#{registration.form.placeholder.name}"/>

            <attr sel="#email_field"
                  th:field="*{email}"
                  th:placeholder="#{registration.form.placeholder.email}"
                  th:errorclass="is-invalid" />
            <attr sel="#email_field_errors"
                  th:each="error: ${#fields.errors('email')}"
                  th:text="${error}"/>

            <attr sel="#password_field"
                  th:field="*{password}"
                  th:placeholder="#{registration.form.placeholder.password}"
                  th:errorclass="is-invalid" />
            <attr sel="#password_field_errors"
                  th:each="error: ${#fields.errors('password')}"
                  th:text="${error}"/>

            <attr sel="#policy_acceptance_checkbox"
                  th:field="*{policyAccepted}"/>
            <attr sel="#policy_acceptance_checkbox_errors"
                  th:each="error: ${#fields.errors('policyAccepted')}"
                  th:text="${error}"/>
            <attr sel="#policy_acceptance"
                  th:text="#{registration.form.policy_acceptance}"/>

            <attr sel="button"
                  th:text="#{registration.form.submit}"/>

            <attr sel="#already_registered"
                  th:text="#{registration.form.alredy_registered}"/>

            <attr sel="#login"
                  th:text="#{registration.form.login}"/>

        </attr>

    </thlogic>
</html>
